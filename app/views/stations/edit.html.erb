<% title "Edit station '#{@station.name}'" %>

<div class="container">
  <div class="row">
    <div class="col">
      <h1>Edit Station <%= @station.name %></h1>

      <%= render 'form' %>

      <%= link_to 'Back', stations_path, { class: 'my-3 d-inline-block' } %>
    </div>
  </div>
</div>
