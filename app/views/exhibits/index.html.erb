<div class="container">
  <div class="row">
    <div class="col">
      <h1>Exhibits</h1>

      <ul>
        <% @exhibits.each do |exhibit| %>
          <li>
            <%= link_to exhibit.id, exhibit %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</div>